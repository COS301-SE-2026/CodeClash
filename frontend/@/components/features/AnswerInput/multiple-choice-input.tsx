import type { InputProps } from "./answer-input";

export const MultipleChoiceInput = ({
    value,
    onChange
}: InputProps) => {

    return (
        <div>
            {value}
        </div>
    )
}